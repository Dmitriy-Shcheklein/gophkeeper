version: v2
modules:
  - path: proto
lint:
  use:
    - STANDARD
  except:
    # Allow request/response names like CreateEntryRequest that do not
    # follow the strict "<Method>Request" convention.
    - RPC_REQUEST_STANDARD_NAME
    - RPC_RESPONSE_STANDARD_NAME
breaking:
  use:
    - FILE
